# componentes/

Web Components nativos (`customElements.define`). Sin framework y sin
dependencias.

- `chip-estado.js` — ya hecho. Lee las etiquetas de `compartido/estados.js`,
  el mismo archivo que usa el servidor.

Candidatos: `campo-peso`, `tarjeta-pedido`, `selector-producto`.
